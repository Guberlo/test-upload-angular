import { S17iDirective } from './s17i.directive';

describe('S17iDirective', () => {
  it('should create an instance', () => {
    const directive = new S17iDirective();
    expect(directive).toBeTruthy();
  });
});
